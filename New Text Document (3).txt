Q1. Write a java file handling program to count and display the number of palindrome word present in a text file "myfile.txt".
Q2. Write a program "Multithreads" that creates two threads, one thread with name "CSThread" and the other thread named "1Thread". Each thread should display its respective name and execute after a gap of 500 milliseconds. Each thread should also display a number indicating the number of times it got a chance to execute.
Q3. Write a Java program to solve producer consumer problem in which a producer produces a value and consumer consumes the value before producer generate the next value.